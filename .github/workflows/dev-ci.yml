name: QA Suite

# Trigger workflow on PRs to DEV and optional pushes (can remove push if not needed)
  push:
    branches:
      - DEV

jobs:
  smoke:
    name: qa/smoke1
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Smoke Tests
        run: |
          echo "Running smoke tests..."
          # Add your smoke test commands here
          sleep 5  # placeholder for actual test commands

  regression:
    name: qa/regression1
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Regression Tests
        run: |
          echo "Running regression tests..."
          # Add your regression test commands here
          sleep 5  # placeholder

  no_run:
    name: qa/no-run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run No-Run Tests
        run: |
          echo "Running no-run tests..."
          # Add commands if needed, or skip
          echo "No tests to run"
